import { DesarrolloShell } from "@/components/desarrollo/DesarrolloShell";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <DesarrolloShell>{children}</DesarrolloShell>;
}
